# Ruff configuration for glass-peer

[lint]
# Ignore rules that are intentionally violated
ignore = [
    # BLE001: Blind exception catches are intentional for network resilience
    "BLE001",
    # S110: close/send on a dead socket has nothing useful to log
    "S110",
]
