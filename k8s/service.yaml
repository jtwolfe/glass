apiVersion: v1
kind: Service
metadata:
  name: glass-inbox
  labels:
    app: glass-inbox
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: glass-inbox
